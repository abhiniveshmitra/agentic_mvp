# Google Gemini API Key (Optional - fallback only)
GOOGLE_API_KEY=your_gemini_api_key_here

# LLM Backend for explainability (phi_local, gemini, mock)
# Default: phi_local (runs Phi-3 locally via Hugging Face)
LLM_BACKEND=phi_local

# NCBI (Optional - for higher rate limits)
# Note: Use ncbi-datasets CLI for data access
NCBI_EMAIL=your_email@example.com
