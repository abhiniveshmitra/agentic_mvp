# Google Gemini API Key (Optional - fallback if OpenAI not set)
GOOGLE_API_KEY=your_gemini_api_key_here

# OpenAI API Key (Recommended for text mining - priority over Gemini)
OPENAI_API_KEY=your_openai_api_key_here

# LLM Provider for text mining: "auto" (default), "openai", or "gemini"
# "auto" uses OpenAI if key is set, falls back to Gemini
LLM_PROVIDER=auto

# LLM Backend for explainability (phi_local, gemini, mock)
# Default: phi_local (runs Phi-3 locally via Hugging Face)
LLM_BACKEND=phi_local

# NCBI (Optional - for higher rate limits)
# Note: Use ncbi-datasets CLI for data access
NCBI_EMAIL=your_email@example.com
